import {FormRowProps} from "@/components/Form/types";

const FormRow = ({children}: FormRowProps) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default FormRow;
